# Config globale pour désactiver la redirection HTTP->HTTPS automatique
{
	auto_https disable_redirects
}

magsav.duckdns.org {
	reverse_proxy localhost:4173
	
	# Logs pour debug
	log {
		output file /tmp/caddy-vehicules.log
		level INFO
	}
}
