fix: audit complet - securite backend, responsive, performances

Securite backend:
- requireAdmin sur POST /api/config/:key (P0: config writable par tous)
- requireAdmin sur DELETE /api/vehicles/:id (P0)
- requireAdmin sur GET access-requests, pending counts, reservation-requests
- requireAdmin sur DELETE attachments/:affaireId/:filename
- requireAdmin sur DELETE clients/drivers/locations/garages/suppliers/orders/quotes
- requireAdmin sur 5 endpoints conges admin-only (leaves, pending, balances, stats)
- Ownership checks sur 6 endpoints conges utilisateur (detail, cancel, sign, justification, pdf, history)
- Remplacement inline isAdmin par middleware requireAdmin sur 4 routes reservations

Responsive:
- LocationDialog: ajout breakpoints 768px + 480px
- MessagingPanel: breakpoint 768px, panel full-width, touch targets 44px
- MaintenanceDialog: breakpoint 768px, form-mode-selector flex-wrap
- EquipmentPanel: menus cascades empiles sur mobile, search input flexible
- AffairesPanel: FAB safe-area, breakpoint 480px
- PersonnelPanel: colonnes planning reduites, touch targets, breakpoint 480px
- Calendar: touch targets toggle/link/trip buttons, webkit-overflow-scrolling
- MobileApp: back button 36px a 44px
- OrdersPanel: icon-btn touch target 44px

Performances:
- Lazy loading Calendar, MobileApp, VehicleDetailsModal (-40% bundle: 597 a 357KB)
- Suppression dead code IndexedDB (deleteFromIndexedDB, clearIndexedDB)

CSS:
- z-index harmonisation (6 fichiers additionnels)
- 100vh to 100dvh (21 occurrences)
- Suppression fichiers orphelins, imports inutilises, routes dupliquees
- Suppression modaux obsoletes (LeaveRequestModal, FlightcasePanel)
